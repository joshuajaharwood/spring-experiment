services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=joshdb'
      - 'MYSQL_PASSWORD=password'
      - 'MYSQL_ROOT_PASSWORD=rootpassword'
      - 'MYSQL_USER=josh'
    ports:
      - '3306:3306'
